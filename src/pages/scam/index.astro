---
import { getCurrentSite } from '../../config/sites.js';

export const prerender = false;

const site = getCurrentSite(Astro.url.host);

// /scam 단독 주소는 각 도메인의 메인 화면으로 이동합니다.
return Astro.redirect('/', 302);
---
